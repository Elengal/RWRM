# Промежуточные файлы сборки RuMod.
# Собранный RuMod.dll в репозитории нужен — он часть мода и уезжает игрокам,
# а вот obj/ пересоздаётся при каждой сборке и в истории только шумит.
RuMod_Source/obj/
RuMod_Source/bin/
*.user

# Мои черновики рядом с проектом
*.bak
